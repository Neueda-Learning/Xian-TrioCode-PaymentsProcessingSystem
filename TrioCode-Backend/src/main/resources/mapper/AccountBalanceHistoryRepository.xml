<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hsbc.triocodebackend.repository.AccountBalanceHistoryRepository">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO account_balance_history
            (account_id, payment_id, operation_type, balance_before, balance_after, amount, description, created_at)
        VALUES
            (#{accountId}, #{paymentId}, #{operationType}, #{balanceBefore}, #{balanceAfter}, #{amount}, #{description}, NOW(3))
    </insert>

</mapper>
