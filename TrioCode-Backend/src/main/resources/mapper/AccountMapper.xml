<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hsbc.triocodebackend.mapper.AccountMapper">

    <select id="selectById" resultType="Account">
        SELECT * FROM account WHERE id = #{id} LIMIT 1
    </select>

    <select id="selectByIdForUpdate" resultType="Account">
        SELECT * FROM account WHERE id = #{id} LIMIT 1 FOR UPDATE
    </select>

    <update id="updateBalance">
        UPDATE account SET balance = #{newBalance} WHERE id = #{id}
    </update>

</mapper>

